---
description: Build a single feature from the project plan
---

Read CLAUDE.md and the project docs.

Implement the feature described in $ARGUMENTS:
1. Check what already exists - do not duplicate
2. Create types/interfaces first
3. Build backend (API routes, database) next
4. Build frontend (pages, components) last
5. Run npm run dev to verify no errors
6. Fix any errors before finishing

Follow all conventions in CLAUDE.md.
